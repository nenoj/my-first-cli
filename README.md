# my-first-cli

Experimental AI-powered developer tooling.

## Features
- AI CLI (`ai`) using OpenAI API
- Express server exposing `/api/ask` and `/api/refactor`
- `/api/refactor` returns unified git diffs suitable for `git apply`

## Usage

```bash
npm install
node server.js



